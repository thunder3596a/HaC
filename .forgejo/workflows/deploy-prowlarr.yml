name: Deploy Prowlarr

on:
  push:
    branches:
      - main
    paths:
      - "Docker-NonCritical/Media/prowlarr/**"
      - ".forgejo/workflows/deploy-prowlarr.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: hac-noncritical
    defaults:
      run:
        shell: sh

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify compose file exists
        run: ls -l "./Docker-NonCritical/Media/prowlarr/prowlarr.yml"

      - name: Compare image and config
        working-directory: "./Docker-NonCritical/Media/prowlarr"
        env:
          PUID: ${{ vars.PUID }}
          PGID: ${{ vars.PGID }}
          TZ: ${{ vars.TZ }}
          DOMAIN_NAME: ${{ vars.DOMAIN_NAME }}
          CERTRESOLVER: ${{ vars.CERTRESOLVER }}
        run: |
          # Extract defined image
          defined_image=$(grep -m1 'image:' prowlarr.yml | awk '{print $2}')

          # Extract running image
          running_image=$(docker inspect -f '{{.Config.Image}}' prowlarr 2>/dev/null || echo "none")

          # Compute checksum of compose file
          checksum=$(sha256sum prowlarr.yml | awk '{print $1}')
          old=$(cat .prowlarr_checksum 2>/dev/null || echo "")

          echo "Defined: $defined_image"
          echo "Running: $running_image"
          echo "Checksum: $checksum vs $old"

          if [ "$defined_image" != "$running_image" ] || [ "$checksum" != "$old" ]; then
            echo "Redeploying Prowlarr"

            # Deploy
            PUID=$PUID PGID=$PGID TZ=$TZ DOMAIN_NAME=$DOMAIN_NAME CERTRESOLVER=$CERTRESOLVER \
              docker compose -p prowlarr -f prowlarr.yml down --remove-orphans || true
            PUID=$PUID PGID=$PGID TZ=$TZ DOMAIN_NAME=$DOMAIN_NAME CERTRESOLVER=$CERTRESOLVER \
              docker compose -p prowlarr -f prowlarr.yml up -d --force-recreate

            # Save checksum
            echo "$checksum" > .prowlarr_checksum
          else
            echo "No redeploy needed"
          fi
