networks:
  toolsproxy:
    external: true
  torrentproxy:
    external: true

services:
  tor:
    image: osminogin/tor-simple:latest
    container_name: searx-tor
    restart: unless-stopped

    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'

    networks:
      - toolsproxy

    labels:
      - ha.monitor=true
      - ha.category=tools
      - ha.compose-file=Docker-NonCritical/Tools/SearX/searx.yml
      - ha.service-name=searx-tor

  searx:
    image: searxng/searxng:latest
    container_name: searx
    restart: unless-stopped
    depends_on:
      - tor

    # Resource limits
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '2.0'
        reservations:
          memory: 256M
          cpus: '0.5'

    environment:
      - TZ=${TZ}
      - HTTP_PROXY=http://gluetun:8888
      - HTTPS_PROXY=http://gluetun:8888
      - http_proxy=http://gluetun:8888
      - https_proxy=http://gluetun:8888
      - NO_PROXY=localhost,127.0.0.1,::1,.onion
      - no_proxy=localhost,127.0.0.1,::1,.onion

    labels:
      # Home Assistant monitoring
      - ha.monitor=true
      - ha.category=tools
      - ha.compose-file=Docker-NonCritical/Tools/SearX/searx.yml
      - ha.service-name=searx
      # Traefik routing
      - traefik.enable=true
      - traefik.http.routers.searx.entrypoints=websecure
      - traefik.http.routers.searx.rule=Host(`search.${DOMAIN_NAME}`)
      - traefik.http.routers.searx.service=searx
      - traefik.http.services.searx.loadbalancer.server.port=8080
      - traefik.http.routers.searx.tls.certresolver=${CERTRESOLVER:-cloudflare}

    networks:
      - toolsproxy
      - torrentproxy

    volumes:
      - /srv/searx/config:/etc/searxng:rw

    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s