# Sudoers configuration for Forgejo runner (nicholas user)
# Install to: /etc/sudoers.d/nicholas-docker
# Usage: sudo visudo -c -f nicholas-docker-sudoers && sudo cp nicholas-docker-sudoers /etc/sudoers.d/nicholas-docker && sudo chmod 0440 /etc/sudoers.d/nicholas-docker

# Docker commands (both /usr/bin and direct paths)
nicholas ALL=(ALL) NOPASSWD: /usr/bin/docker, /usr/bin/docker-compose

# File operations (use wildcards to allow all arguments)
nicholas ALL=(ALL) NOPASSWD: /bin/mkdir, /bin/cp, /bin/chmod, /bin/chown, /bin/touch, /bin/rm, /bin/cat, /bin/tee, /bin/sh, /bin/bash

# Alternative paths for some distros
nicholas ALL=(ALL) NOPASSWD: /usr/bin/mkdir, /usr/bin/cp, /usr/bin/chmod, /usr/bin/chown, /usr/bin/touch, /usr/bin/rm, /usr/bin/cat, /usr/bin/tee, /usr/bin/sh, /usr/bin/bash
